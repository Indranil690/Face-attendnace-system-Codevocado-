<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.faceattendanceapp">

    <!-- ✅ Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ✅ Only include WRITE_EXTERNAL_STORAGE for Android < Q -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- ✅ Declare camera feature -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.FaceAttendanceApp"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- ✅ FileProvider for camera access -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ✅ Activity Declarations -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" android:exported="true" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".RegisterActivity" android:exported="false"/>
        <activity android:name=".UpdateFaceImageActivity" android:exported="false" />
        <activity android:name=".MyAttendanceActivity" android:exported="false" />
        <activity android:name=".AdminDashboardActivity" android:exported="false" />
        <activity android:name=".RequestLeaveActivity" android:exported="false" />
        <activity android:name=".ChangePasswordActivity" android:exported="false" />
        <activity android:name=".ForgotPasswordActivity" android:exported="false" />
        <activity android:name=".ViewEmployeesActivity" android:exported="false" />
        <activity android:name=".AddEmployeeActivity" android:exported="false" />
        <activity android:name=".EditEmployeeActivity" android:exported="false" />
        <activity android:name=".AttendanceOverviewActivity" android:exported="false" />
        <activity android:name=".LeaveRequestsActivity" android:exported="false" />
        <activity android:name=".AdminProfileActivity" android:exported="false" />
        <activity android:name=".EditAdminProfileActivity" android:exported="false" />
        <activity android:name=".AttendanceReportActivity" android:exported="false"/>
        <activity android:name=".AdminAttendanceActivity"  android:exported="false"/>
        <activity android:name=".QRAttendanceActivity"  android:exported="false"/>
        <activity android:name=".AdminFaceAttendanceActivity"  android:exported="false"/>

    </application>
</manifest>
